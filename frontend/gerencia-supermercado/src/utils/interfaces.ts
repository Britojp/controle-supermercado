export type States = {
    id: string,
    name:string,
    uf:string
}
export type Meansurement = {
  id: string
  name: string
  initials: string
}

export type Shelves = {
  id: string
  name: string
}

export type Corridors = {
  id: string
  name: string
}